# @pizza-doc/web

Private package: it is never published and carries no changesets of its own.
Its version follows the published packages via `scripts/sync-versions.mjs`;
see the root [CHANGELOG.md](../../CHANGELOG.md) for what changed in each
release.
